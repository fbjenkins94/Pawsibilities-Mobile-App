                    renderItem={(item) => {
                        return (
                            <Pressable onPress={ navigation.navigate('HomeScreen')}>{renderItem(item)}</Pressable>
                        );
                    }}